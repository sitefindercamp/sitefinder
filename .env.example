NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Comma-separated list of email addresses that can access /admin.
# All other authenticated users are routed to /owner/dashboard.
ADMIN_EMAILS=

# Optional. Used to build absolute URLs for magic-link redirects.
# Defaults to deriving from request headers if unset.
NEXT_PUBLIC_SITE_URL=
